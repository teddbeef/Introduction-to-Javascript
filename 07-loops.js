const names = ["Teddy", "Cathy", "Mixu", "Tommy"];

for (let i = 0; i < names.length; i++){
    console.log(names[i]);
}

const numbers = [1, 2, 3, 4, 5];
for (let j = 0; j < numbers.length; j++){
    console.log(numbers[j] * 2);
}